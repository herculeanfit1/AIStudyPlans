# ==============================================================================
# SchedulEd (AIStudyPlans) — 1Password Secret Injection Template
# ==============================================================================
# Used with: op run --env-file=.env.1p.template -- <command>
# Vault:     BTAI-CC-SchedulEd
# Safe to commit — contains only op:// references, not actual secrets.

# === Authentication (NextAuth + Azure AD) ===
NEXTAUTH_SECRET="op://BTAI-CC-SchedulEd/NextAuth/secret"
NEXTAUTH_URL=http://localhost:3000
AZURE_AD_CLIENT_ID="op://BTAI-CC-SchedulEd/Azure AD/client-id"
AZURE_AD_CLIENT_SECRET="op://BTAI-CC-SchedulEd/Azure AD/client-secret"
AZURE_AD_TENANT_ID="op://BTAI-CC-SchedulEd/Azure AD/tenant-id"

# Comma-separated list of email addresses allowed to access /admin
ADMIN_EMAILS="op://BTAI-CC-SchedulEd/Admin/emails"

# === Database (Supabase) ===
NEXT_PUBLIC_SUPABASE_URL="op://BTAI-CC-SchedulEd/Supabase/url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="op://BTAI-CC-SchedulEd/Supabase/anon-key"
SUPABASE_SERVICE_ROLE_KEY="op://BTAI-CC-SchedulEd/Supabase/service-role-key"

# === Email (Resend) ===
RESEND_API_KEY="op://BTAI-CC-SchedulEd/Resend/api-key"
EMAIL_FROM="Lindsey <lindsey@aistudyplans.com>"
EMAIL_REPLY_TO=support@aistudyplans.com
SEND_EMAILS_IN_DEVELOPMENT=true

# === Feedback Campaign ===
FEEDBACK_CAMPAIGN_API_KEY="op://BTAI-CC-SchedulEd/Feedback Campaign/api-key"

# === Application (non-secret, static) ===
NEXT_PUBLIC_APP_URL=http://localhost:3000
TZ=America/Chicago

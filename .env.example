# Authentication (NextAuth)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Azure AD (NextAuth provider)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# Admin Access
# Comma-separated list of admin email addresses
ADMIN_EMAILS=admin@example.com

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # Server-side only, never expose to client

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM="SchedulEd <noreply@example.com>"
EMAIL_REPLY_TO=support@aistudyplans.com
SEND_EMAILS_IN_DEVELOPMENT=true

# Feedback Campaign
FEEDBACK_CAMPAIGN_API_KEY=        # Bearer token for cron-triggered feedback endpoint

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Development
DISABLE_RATE_LIMIT=false          # Set to true to disable rate limiting in dev

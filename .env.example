# === Authentication (NextAuth + Azure AD) ===
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# Comma-separated list of email addresses allowed to access /admin
ADMIN_EMAILS=admin@example.com

# === Database (Supabase) ===
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=              # Server-side only, never expose to client

# === Email (Resend) ===
RESEND_API_KEY=
EMAIL_FROM="SchedulEd <noreply@example.com>"
EMAIL_REPLY_TO=support@aistudyplans.com
SEND_EMAILS_IN_DEVELOPMENT=true

# Email rate limits (defaults shown, all optional)
# EMAIL_DAILY_LIMIT=100
# EMAIL_MONTHLY_LIMIT=1000
# EMAIL_HOURLY_LIMIT=10
# EMAIL_MAX_FAILURES=5                  # Consecutive failures before circuit breaker trips
# EMAIL_CIRCUIT_BREAKER_TIMEOUT=300000  # Cooldown in ms (default 5 min)

# === Feedback Campaign ===
FEEDBACK_CAMPAIGN_API_KEY=              # Bearer token for cron-triggered feedback endpoint

# === Application ===
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === Development (optional) ===
# DISABLE_RATE_LIMIT=false              # Set to true to disable rate limiting in dev
# DEBUG_EMAIL=false                     # Set to true to enable verbose email logging
